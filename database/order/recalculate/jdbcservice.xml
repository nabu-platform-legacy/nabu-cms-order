<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>update orders set 
	price = (select sum(price) from order_entries where order_id = :orderId),
	weight = (select sum(weight) from order_entries where order_id = :orderId and weight is not null)
where id = :orderId
</sql>
</jdbcService>